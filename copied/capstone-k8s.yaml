apiVersion: apps/v1
kind: Deployment
metadata:
  name: django-capstone-app-ifeanyichukwu
  labels:
    app: django-capstone-app-ifeanyichukwu
spec:
  selector:
    matchLabels:
      app: django-capstone-app-ifeanyichukwu
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  progressDeadlineSeconds: 1200
  minReadySeconds: 60
  template:
    metadata:
      labels:
        app: django-capstone-app-ifeanyichukwu
    spec:
      terminationGracePeriodSeconds: 60
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: "app"
                operator: In
                values:
                - django-capstone-app-ifeanyichukwu
              - key: "temporary"
                operator: In
                values:
                - "true"
            topologyKey: "kubernetes.io/hostname"
      containers:
      - name: udacity
        image: angeloobeta/django-capstone-app-ifeanyichukwu
        command: [
          "/bin/bash", 
          "-c", 
          "npm run serve"
          ]
        imagePullPolicy: Always
        resources:
          limits:
            memory: 64Mi
            cpu: 250m
          requests:
            memory: 32Mi
            cpu: 200m
        ports:
        - containerPort: 9080
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /
            scheme: HTTP
            port: 9080
          periodSeconds: 10
          successThreshold: 1
          failureThreshold: 2
          timeoutSeconds: 1

---
apiVersion: v1
kind: Service
metadata:
  name: django-capstone-app-ifeanyichukwu
spec:
  type: LoadBalancer
  selector:
    app: django-capstone-app-ifeanyichukwu
  ports:
    - name: http
      protocol: TCP
      port: 9080
      targetPort: 9080
    - name: https
      protocol: TCP
      port: 443
      targetPort: 443
      
---
kind: HorizontalPodAutoscaler
apiVersion: autoscaling/v1
metadata:
  name: django-capstone-app-ifeanyichukwu
spec:
  scaleTargetRef:
    apiVersion: extensions/v1beta1
    kind: Deployment
    name: django-capstone-app-ifeanyichukwu
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80

---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: django-capstone-app-ifeanyichukwu
  annotations:
    ingress.kubernetes.io/force-ssl-redirect: "true"
    kubernetes.io/ingress.class: "ingress-nginx"
    #Limit app to my personal IP
    #nginx.ingress.kubernetes.io/whitelist-source-range: "IP/32"

spec:
  rules:
  - host: bcrypt.darkarosh.net
    http:
      paths:
      - path: /
        backend:
          serviceName: django-capstone-app-ifeanyichukwu
          servicePort: 9080
